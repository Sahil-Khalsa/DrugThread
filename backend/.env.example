# Strands Agents — LLM provider (OpenAI, direct)
# Get from platform.openai.com — never commit the real value, never paste it in chat.
OPENAI_API_KEY=

# Bright Data — investigation/gap-filling layer (spec §15)
BRIGHT_DATA_API_KEY=

# Convoke — biopharma evidence/knowledge layer (spec §16)
# Accessed via MCP (https://mcp.convoke.bio/mcp) instead of a REST key for now.
# Re-add a key/token here if the MCP connection turns out to need one once we test it.

# openFDA and ClinicalTrials.gov are public and need no key for hackathon-scale usage.
